# Protect internal source code
RewriteEngine On
RewriteRule ^src/ - [F,L]

# Allow scripts for now, but you may want to protect this folder 
# with Basic Auth or IP restriction later for production.
# RewriteRule ^scripts/ - [F,L]
